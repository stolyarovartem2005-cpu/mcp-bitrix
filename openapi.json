{
  "openapi": "3.0.1",
  "info": {
    "title": "MCP Bitrix Connector",
    "version": "1.0.0",
    "description": "API-проксі для інтеграції між ChatGPT і Bitrix24 через Render-сервер."
  },
  "servers": [
    {
      "url": "https://mcp-bitrix.onrender.com"
    }
  ],
  "paths": {
    "/health": {
      "get": {
        "summary": "Перевірка стану сервера",
        "responses": {
          "200": {
            "description": "Сервер працює"
          }
        }
      }
    },
    "/bitrix": {
      "post": {
        "summary": "Виконати запит до Bitrix24 через сервер-проксі",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": {
                  "method": {
                    "type": "string",
                    "example": "crm.lead.list"
                  },
                  "params": {
                    "type": "object",
                    "example": {
                      "select": ["ID", "TITLE"],
                      "filter": {"STATUS_ID": "NEW"}
                    }
                  }
                },
                "required": ["method"]
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Відповідь Bitrix24 API"
          }
        }
      }
    }
  }
}
